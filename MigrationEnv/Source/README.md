# Legacy Campaign Source

Copy all of your old `.md` campaign files into this folder. It doesn't matter if they are poorly formatted or sorted chaotically. 

Once your files are here, ask Antigravity to **"Run the migration workflow"** or type `/migrate_campaign`.

The AI will read these files, clean them up, format them into the new JSON templates, and save the results in the `Destination` folder.
