# New Campaign

Run the `new_campaign` workflow for the current campaign.

When invoked, load `Campaign/.agents/workflows/new_campaign.md` and execute its steps sequentially, asking for any required inputs.

