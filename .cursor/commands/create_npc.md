# Create NPC

Run the `create_npc` workflow to build a new non-player character for the current campaign.

When invoked, load `Campaign/.agents/workflows/create_npc.md` and execute its steps sequentially, asking for any required inputs.

