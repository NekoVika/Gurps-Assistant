# Prep Session

Run the `prep_session` workflow to prepare the next game session.

When invoked, load `Campaign/.agents/workflows/prep_session.md` and execute its steps sequentially, asking for any required inputs.

