# Brainstorm

Run the `brainstorm` workflow to generate ideas for the current campaign.

When invoked, load `Campaign/.agents/workflows/brainstorm.md` and execute its steps sequentially, asking for any required inputs.

