# Conclude Session

Run the `conclude_session` workflow to wrap up the current session and update state.

When invoked, load `Campaign/.agents/workflows/conclude_session.md` and execute its steps sequentially, asking for any required inputs.

