# Start Session

Run the `start_session` workflow to begin a live play session.

When invoked, load `Campaign/.agents/workflows/start_session.md` and execute its steps sequentially, asking for any required inputs.

